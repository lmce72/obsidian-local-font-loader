# Changelog

<!--
  The text directly under a version heading — before the first `###` — becomes the GitHub Release
  body, so it is read by users rather than by developers. Keep it short and free of build jargon;
  everything below it is the developer record and never reaches the release.
-->

## [1.5.8] - 2026-09-26

修复导出 PDF 时字体不生效的问题：此前 PDF 里的正文、标题、代码与数学公式都会回退成默认字体，现在会使用你所配置的字体。

### 🐞 修复

1. **导出 PDF 未使用所配置字体**
   - 原因：导出 PDF 会另开一个窗口，只把主文档 `<head>` 里的 `<style>` 元素搬进去；而插件自 1.5.4 起改用的可构造样式表（`adoptedStyleSheets`）不是 DOM 节点，永远搬不过去 —— 于是屏幕上正常、PDF 里全部落空
   - 拉丁字体与数学字体都受影响，两者的 CSS 都是运行时生成的
   - 改为由 CSS 片段承载生成的 CSS：Obsidian 会把它注入为 `<head>` 里的 `<style>`，因而既能作用于主窗口，也能被 PDF 导出带走；缺少可构造样式表的旧版 iOS 亦由同一载体覆盖

2. **片段文件的位置与清理**
   - 生成物位于 `.obsidian/snippets/local-font-loader.css`（内嵌字体，体积可达数十 MB），由插件自动启用，随字体配置变化而重写
   - 插件卸载或停用时只停用该片段、不删除文件；设置页的「清空缓存」会连同它一起移除

## [1.5.7] - 2026-09-25

修复一处会清空全部配置的严重问题：同步之后插件可能显示为全新安装。若你的插件目录里同时存在 `local-font-loader` 与 `obsidian-local-font-loader` 两个文件夹，请只保留前者，把另一份移出 `plugins` 目录。同时修复移动端无法转换 Base64 字体，并改进目录设置。

### 🐞 严重问题修复

1. **同步后配置被重置为默认**
   - 启动时若尚未读到 `data.json`（同步还没落地），插件不再把内存里的默认值写回磁盘 —— 那份快照会经同步覆盖掉所有设备的真实配置
   - 配置迁移不再替换预设列表：默认预设字体为空是合法状态（设备预设接管字体时本就如此），不再据此判定配置损坏并清空全部预设
   - 两个缺陷叠加时，一次启动就足以让全网配置回到「全新安装」的样子

2. **插件目录重名（需手动处理）**
   - 从社区商店安装会生成与插件 id 同名的 `local-font-loader`，而早前手动拷贝留下的 `obsidian-local-font-loader` 声明的是同一个 id
   - Obsidian 每个 id 只加载一个，且两者各有各的 `data.json`：表现为同一插件时而读到真实配置、时而读到默认值，且不同设备可能加载到不同的一份
   - 只保留 `local-font-loader`，把另一份移出 `plugins` 目录即可；本版无法代为处理目录本身

### 📱 移动端

3. **Base64 转换失败**
   - 缓存目录此前从未被创建，而 `mkdir` 只建一级；配置成 `Components/Library/Fonts/B64Font` 这类多级路径时，移动端必然失败（桌面端恰好手动建过目录才未暴露）
   - 改为逐级建立缺失的父目录，扫描、导入与转换三处统一走同一实现
   - 已在 Android 真机验证：多级目录可建立、转换写入正常

### ⚙️ 设置界面

4. **目录设置支持联想与浏览**
   - 输入时联想库中已有的文件夹，并新增「浏览」按钮直接选取

5. **设备列表自我修复**
   - 同一设备因标识变化被重复登记时自动合并，预设绑定与显示名一并迁移
   - 同型号设备因无法可靠区分而不自动合并，只给出判断依据（重叠/接替/未知）

6. **内联样式并入 `styles.css`**
   - 折叠、拖拽、校验、选中态等动态样式改用 class 切换，同时消除社区审核的对应告警

7. **日志统一**
   - 控制台错误一律经插件日志函数输出，不再散落裸 `console` 调用

## [1.5.6] - 2026-09-23

修复社区插件审核指出的问题：插件不再自行建立 `<style>` 元素。旧款 iOS 上的字体改由 Obsidian 自己载入，功能不受影响。

### 🔧 相容性

1. **生成的样式表不再经由 `<style>` 元素**
   - 支援可构造样式表的平台（Safari 16.4 / Chromium 73 起，含桌面与现行移动端）仍走 `document.adoptedStyleSheets`，行为与 1.5.4 起相同
   - 仅 iOS 15.6–16.3 等缺少该 API 的 WebView 改走 CSS 片段：插件把生成的 CSS 写入片段文件夹并经 `app.customCss` 启用，由 Obsidian 载入 —— 即审核建议的 `styles.css` 机制
   - 片段写入按序串接，两次套用（`@font-face` 与变量）合并为一次写入，不会写入半新半旧的内容
   - 改用可构造样式表的平台上会回收先前遗留的片段（含经同步传来的其他设备片段）；非本插件写入的文件只停用、不删除

### 📝 审核说明

2. **报错行已消除**
   - 报错位置 `src/plugin.ts:2668`（1.5.5）是 `applyCss` 中为旧 WebView 保留的 `<style>` 回退分支；该分支已删除，全仓库无任何建立或挂载 `<style>` 的代码
   - 回退改以 CSS 片段实现，因此 iOS 15.6–16.3 的字型支援未被牺牲

## [1.5.5] - 2026-09-16

修复跨设备识别的稳定性问题，建议更新。

### 🔧 相容性

1. **navigator 改以别名访问**
   - 两处用途（Android 机型解析、旧版迁移杂凑的冻结输入）保持不变
   - 模块级别名 `browserNavigator` 取代逐处 `navigator.<property>` 写法
   - 所有作业系统判断仍由 Platform 或 process.platform 决定，不经过 navigator

### 📦 发布

2. **新增产物仓库**
   - 构建产物同步至 obsidian-local-font-loader-release，由 `bun run publish-release` 推送
   - 源码仓库不受影响

## [1.5.4] - 2026-09-16

改进了字体加载方式与跨设备识别的稳定性，建议更新。

### 🔧 相容性

1. **產生的樣式表改用可構造樣式表**
   - `applyCss` 改以 `document.adoptedStyleSheets` 套用執行時產生的字形 CSS
   - 不再建立 `<style>` 元素；未實作該 API 的 WebView 仍走元素回退路徑
   - 按 id 記錄已套用的樣式表，避免重複套用與洩漏

### 📝 審核說明

2. **三處刻意保留的例外，已就地註明理由**
   - `_detectDeviceInfo`：讀取 User-Agent 僅用於解析 **Android 機型**，
     Obsidian 未提供此 API，UA 是唯一來源；所有作業系統判斷皆由 `Platform`
     或 `process.platform` 決定，不經 UA
   - `_generateDeviceFingerprint`：此雜湊為**舊版遷移鍵**，輸入值刻意凍結。
     更動會使已安裝裝置找不到自己先前建立的條目，重新註冊成重複裝置
   - `applyCss`：`adoptedStyleSheets` 需 Safari 16.4+，回退路徑供舊 WebView 使用，
     否則那些裝置上字型將完全無法載入

## [1.5.3] - 2026-09-16

提升与 Obsidian 的兼容性（弹出窗口、社区插件审核要求），并修复若干问题，建议更新。

### 🐛 Bug 修復

1. **修復彈出視窗中的計時器與動畫不生效**
   - `setTimeout` / `clearTimeout` / `requestAnimationFrame` 未掛在 `window` 上，
     在彈出視窗中不作用

2. **修復桌面端系統判定依賴 User-Agent**
   - 改由 `process.platform` 判定 Windows / macOS / Linux，更可靠
   - 僅在 Node 不可用時才回落到 UA

3. **修復外掛實例被當作渲染元件使用**
   - `MarkdownRenderer` 的 Component 生命週期原本橫跨整個工作階段，
     設定頁每次重繪都會殘留渲染產物
   - 改為每次渲染建立短生命週期元件，隨設定頁重繪一併卸載

### 🔧 相容性

4. **宣告真實所需的最低版本**
   - `minAppVersion` 由 `0.15.0` 更正為 `1.13.0`
   - 程式實際使用 `loadLocalStorage`（1.8.7）、`ConfirmationModal` 與
     `Modal.setTitle` / `addButton`（1.13.0）

5. **設定頁樣式表交由 Obsidian 載入**
   - 原本於執行時建立 `<style>` 元素注入，現併入 `styles.css`

6. **靜態樣式改以 `setCssStyles` 設定**，不再逐條賦值 `element.style`

7. **設定頁標題改用 `Setting.setHeading()`**，不再直接建立 `h2`

## [1.5.2] - 2026-09-16

默认字体目录改为独立的 `Local-Fonts/`，新安装开箱即用；同时修复若干问题，建议更新。

### 🔧 工程化

1. **轉為 Bun 構建專案**
   - `main.js` 由手寫單檔改為構建產物，原始碼遷入 `src/`
   - 提供 `bun run build` / `dev` / `typecheck` / `sync-version`
   - 版本以 `package.json` 為唯一來源，同步寫入 `manifest.json` 與入口橫幅
   - CI 於發布前安裝依賴、型別檢查、再從 `src/` 構建，確保產物與 tag 對應的原始碼一致

2. **全量遷移至 TypeScript**
   - 新增 `src/types.ts` 集中宣告持久化資料形狀：字型、家族、預設、裝置、數學度量
   - 新增 `src/obsidian-extras.d.ts` 集中宣告 Obsidian 未公開但執行時存在的成員
   - 型別檢查由 549 個錯誤收斂至 0

3. **拆分臃腫的設定頁**
   - `display()` 原為單一 1416 行方法，現拆為四個章節模組
     （裝置與預設管理／目錄設定與字型應用／字型檔案狀態／備用操作）與一個樣式模組
   - 設定頁主檔由 2110 行降至 754 行
   - 樣式表改為按 id 守衛只注入一次，不再隨每次重渲染重建

### 🐛 Bug 修復

4. **修復刪除預設時拋錯**
   - `deletePreset` 以 `this.getTranslation(key)` 覆蓋了 i18n 的 `t`，但外掛並無此方法
   - 刪除預設時會拋 `TypeError`，而非顯示提示

5. **修復重建預設時靜默丟失設定**
   - `_ensureDevicePreset` 從 `settings` 讀取 `latinFontEnabled`、`latinFontScope`、
     `headingApplyToFileTitle`，但這三個欄位屬於 preset，讀到的永遠是 undefined

6. **修復快取目錄被當作字型家族掃描**
   - 掃描時以硬編碼名稱 `B64Font` 排除快取目錄
   - 快取目錄改名後（預設即位於來源目錄內）會被當成一個字型家族
   - 現改為依 `b64OutputDir` 的設定名稱判斷

7. **修復來源目錄不存在時掃描失敗**
   - 改為按需建立目錄，而非讓 `list()` 拋錯

### ✨ 變更

8. **預設字型目錄改用獨立資料夾**
   - `fontSourceDir`：`Components/Library/Fonts` → `Local-Fonts`
   - `b64OutputDir`：`Components/Library/Fonts/B64Font` → `Local-Fonts/UsableCssFont`
   - 外掛是給使用者用的，預設值不應寫入屬於 vault 其它部分的目錄
   - 既有 `data.json` 不受影響：預設值僅在鍵缺失時生效

9. **清除 i18n 中的重複鍵**
   - 6 處重複鍵，其中 `usingGlobalPreset` 兩處文案不同，先出現的一條從未生效

## [1.5.1] - 2026-09-16

### 🐛 Bug 修復

1. **修復數學字體度量未真正套用**
   - `@font-face` 宣告了不等於已載入：瀏覽器要等到有元素實際用它繪製時才會去取得字體檔
   - 度量檢測發生在這個載入之前，會被靜默跳過，於是在部分環境下整個度量修正形同未執行
   - 現在先強制載入字體面再進行測量，並於載入後複核，確保量到的不是回退字體

2. **標籤頁頭部套用 UI 字體**
   - 納入 `.view-header-title-container`、`.view-header-title-parent`、
     `.view-header-breadcrumb`、`.view-header-breadcrumb-separator`、`.view-header-title`
   - 該區塊顯示目前筆記的資料夾路徑與標題，屬於介面外殼而非文件內容

## [1.5.0] - 2026-09-15

### ✨ 新增

1. **自訂數學字體改為修正 MathJax 的度量表**
   - MathJax 不在執行時讀取字體檔，而是用一張離線預製的度量表排版，它產生的 CSS
     只是這張表的投影。過去只換了「用哪個字體畫字形」，沒換「按什麼尺寸排」，
     導致字形來自使用者的字體、幾何卻來自 MathJax 的字體
   - 現在改用 canvas 逐字量出所選字體**實際渲染**的度量，寫回
     `MathJax.config.chtml.font`（24 個變體、3391 個字形）與 `delimiters` 的 HDW，
     再讓 MathJax 用它自己的機制重算幾何
   - 度量只隨字體變更計算一次，之後渲染不增加任何額外開銷
   - 外掛卸載或更換字體時會還原 MathJax 的原始度量（該物件為全域共用）

2. **數學字體合規檢測**
   - 設定頁在數學字體旁顯示警告圖示，並以警告框列出偏離的度量項目
   - 判據取自 MathJax 自帶 TeX 字體的度量（Computer Modern 體系）

3. **筆記標題與屬性面板套用 UI 字體**
   - `.inline-title`、筆記屬性面板與標籤一併納入 UI 字體範圍

### 🐛 Bug 修復

4. **修復根號橫槓與鉤子脫開**
   - MathJax 為 `√` 預留的空間（上下各 0.8em / 0.2em）是照它自己字體的墨跡算的
   - 換字體後墨跡高度不同，橫槓因此浮在鉤子上方
   - 度量表修正後，預留空間等於字體實際墨跡

5. **修復上下標與底數浮離**
   - 排版依度量表為每個斜體變數預留寬度，換字體後寬度不符，上下標逐層累積偏移

6. **修復下花括號斷成一串方塊**
   - 拉伸分隔符由**私有區碼位**拼成（如 `\underbrace` 使用 U+E152/E153/E154/E156）
   - 這些字形只存在於 MathJax 的 TeX 字體，任何一般數學字體都沒有
   - 改為讓拼接件維持 MathJax 自身的字體

7. **修復重新掃描／轉換字體後下拉選單不更新**
   - 兩個操作只更新了資料卻未重繪設定頁，新字體因此不出現在選單中

### 🔧 其他

- 拉伸尺寸變體（TEX-S1 ~ TEX-S4）不再被字體覆寫規則命中，以免高伸內容的幾何被破壞
- UI 字體規則涵蓋元素由 16 個增至 27 個以上

## [1.4.0] - 2026-09-14

### ✨ 新增

1. **設備預設命名改用「設備本身叫什麼」**
   - 桌面端使用系統主機名，行動端使用機型（如 `PJZ110`、`iPad`）
   - 原本多台機器一律顯示為 `Desktop-Linux`、`Mobile-Android`，無法分辨
   - 兩者皆取不到時才回退為「平台 + 系統」形式
   - 僅自動生成的名稱會隨規則更新，使用者自訂的名稱永不被覆蓋

2. **設備副行顯示主機名／機型**
   - 即使設備已被重新命名，仍能看出它實際是哪一台機器
   - 顯示名若已等於該標識則不重複顯示

3. **新增 iPadOS 識別**
   - 系統判定區分為 iOS、iPadOS、macOS 三態，並各自對應圖示
   - 使用 Obsidian 的 `Platform.isIosApp` 與 `Platform.isTablet`，而非 UA 嗅探
   - iPadOS 13 起 iPad 會自報桌面級 `Macintosh` UA，純 UA 嗅探必然誤判為 macOS

### 🐛 Bug 修復

4. **修復通知（Notice）等浮動 UI 未套用 UI 字體**
   - `.notice` 不在 UI 字體規則內，走繼承鏈落到 Obsidian 核心介面字體
   - 同類浮動 UI 一併納入：tooltip、命令面板、補全建議、懸浮預覽、編輯器補全面板

5. **修復 Android 無機型 UA 被誤解析出 `Linux`**
   - 原解析倒序掃描區塊，把同區塊中的 `Linux` 當成機型
   - 改為錨定 `Android <版本>` 段，取其後首個有效段（跳過語言標記與 `wv`）

6. **修復同型號設備可能共用同一設備 ID**
   - 指紋由 UA、解析度、時區、語言、核心數雜湊而成，同型號設備完全相同
   - 遷移時兩台設備會認領到同一 ID 並永久混為一台
   - 改為「認領即封口」：設備取得 ID 後，帳本中指向該 ID 的條目立即刪除
   - 已持有 ID 的設備在啟動時同樣封口，避免舊版本遺留條目被他人認領

7. **修復設備本地 ID 寫入失敗未被察覺**
   - 寫入後未校驗，靜默失敗會導致每次啟動都換新 ID
   - 現在寫入後立即回讀比對，不一致則明確輸出錯誤

8. **修復元信息中已廢棄欄位永不清除**
   - 結構變更（如移除 `osVersion`）不再參與比較，舊欄位會永久殘留
   - 現在鍵集合不一致即視為變更並重寫

### 🔧 其他

- 移除系統版本號：不再偵測、儲存或顯示，升級系統不會再造成元信息變動
- UI 字體規則統一為共用模板 `_buildUiFontRules()`，覆蓋元素由 16 個增至 27 個
- 設備身份改以唯一標識為唯一判準，名稱／機型僅用於顯示，不參與匹配

## [1.3.0] - 2026-09-14

### ✨ 新增

1. **設備唯一標識改為持久化 UUID**
   - 舊版以「UA + 螢幕解析度 + 時區 + 語言 + CPU 核心數」雜湊作為身份
   - 這些因子在旋轉、分屏、DPI 變化、夏令時下都會漂移，導致幾乎每次啟動都註冊一台「新設備」
   - 現在改為首次執行時生成 UUID 並存入**設備本地存儲**（不隨 data.json 同步），重啟不變
   - 舊指紋表僅作一次性遷移線索，已安裝的設備保留原有 ID 與預設綁定

2. **桌面端預設設備名改用主機名**
   - 多台桌機原本一律顯示為 `Desktop-Linux`／`Desktop-Windows`，無法分辨
   - 現在桌面端讀取系統主機名作為預設名稱；僅自動生成的名稱會隨升級刷新，使用者自訂的名稱不會被覆蓋

3. **設備型號顯示**
   - 設備條目新增型號副行：Android 顯示 UA 機型（如 `SM-G991B`），iOS 顯示機型族與系統版本（如 `iPhone · iOS 17.0`）
   - 作業系統圖示改為讀取設備自報的元信息，不再從設備名推斷

4. **新增「清理未綁定設備」入口**
   - 位於全域預設區，附確認對話框並逐條列出將被移除的設備
   - 僅清理非本機且未被任何預設綁定的條目

### 🐛 Bug 修復

5. **修復移動端行內代碼未套用代碼字體**
   - 使用者 CSS 片段以 `:root` 前綴取得更高特異性，壓過插件的 `body …` 規則
   - 片段硬編碼的字體名在移動端不存在，回退後落到系統通用 `monospace`

6. **修復移動端多行代碼塊行號未套用代碼字體**
   - 行號元素此前完全沒有規則，繼承的是正文文字字體
   - 現已涵蓋 Code Styler 行號與編輯器原生行號（`.cm-gutterElement`）

7. **修復 `var(--font-monospace)` 未生效**
   - Obsidian 核心把外觀字體設定寫成 `<body>` 的**內聯樣式**，而插件僅在 `:root` 宣告同名變數
   - `:root` 對 `<body>` 及其後代不構成競爭，導致所有讀取該變數的規則都解析到核心設定值
   - 現在等寬變數同時宣告於 `<body>` 作用域

8. **修復 Android／iOS 設備被誤判為 Linux／Mac**
   - Android 的 UA 同時含 `Linux`、iOS 的 UA 同時含 `Mac OS X`
   - 判定順序導致 `Android`／`iOS` 兩個分支永不可達，設備一律被命名為 `Mobile-Linux`
   - 現在 iOS 先於 macOS、Android 先於 Linux 判定

### 🔧 其他

- 統一程式碼字體規則的產生邏輯：行內代碼、代碼塊、行號由同一模板輸出，優先級只在一處決定
- 移除程式碼註解中的中文，改為純英文註解

## [1.2.5] - 2026-08-28

### 🐛 Bug 修復

1. **修復插件升級後配置不熱重載**
   - 升級版本後需重新啟動才能套用新配置
   - 現在會自動偵測配置變更並重新載入

## [1.2.4] - 2026-08-21

### 🐛 Bug 修復

1. **修復「轉換所有字體」功能失效**
   - 轉換流程發生錯誤但介面仍顯示「✓ 所有字體已轉換」的成功提示
   - 現在會正確輸出每個字體的實際轉換結果與錯誤訊息

2. **修復刪除目前編輯中的預設後設定頁消失**
   - 刪除正在編輯的預設後，設定頁下半部不再空白
   - 自動回退到目前設備的預設並完整渲染

3. **修復關閉設定頁後同步不再自動刷新**
   - 關閉設定頁後，雲端同步的變更不再自動顯示
   - 重新開啟設定頁即正確反映最新狀態

4. **修復複製預設時克隆錯誤內容**
   - 複製任何預設都會誤克隆「目前設備的預設」
   - 現在正確複製所選預設的內容

5. **修復特殊字元字體名稱破壞樣式**
   - 字體名稱含引號或反斜線時，可能破壞 CSS 樣式
   - 現在會自動轉義，安全生成 `@font-face` 與 `font-family`

6. **修復重新掃描時缺失字體被靜默移除**
   - 來源檔案被刪除的字體在重新掃描後不再消失
   - 改為保留條目並標示「缺失」狀態，供使用者手動處理

7. **修復刪除「僅快取」字體失敗**
   - 來源檔案已不存在的字體無法被刪除，殘留於列表
   - 現在可正常清理快取與條目

8. **修復儲存設定與同步重載的競態**
   - 快速修改多項設定時可能被舊資料回滾，或輸入框焦點丟失
   - 加入儲存中守衛，避免重載干擾正在編輯的內容

9. **統一 Latin 字體判定邏輯**
   - 單獨轉換與批次轉換的 unicode-range 判定不再不一致
   - 一律以目前設備預設的 Latin 字體設定為準

10. **多項資料防禦與資源清理**
    - 設定資料缺失 `fonts` 欄位時不再崩潰
    - 插件卸載時徹底清除注入的樣式與圖示

---

## [1.2.1] - 2026-08-21

### 📦 重新打包正式版本

1. **修復正式版 Release 資產過期問題**
   - GitHub 上原有的 `v1.2.0` Release 建立於設備管理重構之前，資產為舊版程式碼
   - `v1.2.1` 重新打包全部最新程式碼，確保資產與原始碼一致

### 🌏 文件更新

1. **README 新增繁體中文版本**
   - 完整繁体中文章節（概覽、功能、安裝、使用、效能、設定、授權等）
   - 頂部語言切換欄支援 6 種語言

### 🔐 發佈流程

1. **新增 GitHub Actions 發佈簽章流程**
   - 推標籤時自動建立 GitHub Release 並上傳插件資產
   - 使用 `attest-build-provenance` 為資產附加加密來源證明（Artifact Attestations）

### 📝 程式碼規範

1. **全英文註釋**
   - 所有程式碼註釋維持全英文樣式，符合專案規範

---

## [1.2.0] - 2026-08-21

### 🛠️ 設備管理重構

1. **設備與預設一對一綁定**
   - 一台設備僅能關聯且僅能關聯一個預設
   - 移除設備時自動從所有預設中清除關聯

2. **新設備自動註冊**
   - 插件啟動時偵測到與上次不同的設備，自動產生新設備並寫入 `data.json`
   - 無需手動重新載入插件即可在跨設備之間同步

3. **設備識別改用裝置指紋（平台 + UA/螢幕/時區/語言/CPU 雜湊）**
   - 設備 ID 持久化儲存在 `data.json`（可隨 vault 雲端同步）
   - 捨棄以 localStorage 作為設備標識的臨時方案
   - 支援多桌面端、多手機、多平板，無寫死邏輯

4. **修復同一物理設備出現重複 / 幽靈條目**
   - 清理 `deviceNameMap` 中重複的設備紀錄
   - 已識別設備名稱遺失時自動恢復

### 🔄 同步與刷新

1. **data.json 即時監聽**
   - 雲端同步系統推送 `data.json` 後，設備管理無需重啟插件即自動刷新

2. **設備管理工具列新增「重新整理」按鈕**
   - 使用 `refresh-cw` Lucide 圖示，可手動強制重新載入設定並重新渲染

### 🎨 UI 修復

1. **非當前設備顯示作業系統圖示**
   - 依設備平台類型（桌面 / 行動）與設備名稱推斷並顯示對應 OS 圖示

2. **「移動」按鈕圖示改為白色**
   - 與 Obsidian 原生按鈕樣式一致，hover 時恢復強調色

### 📝 程式碼規範

1. **全英文註釋**
   - 所有程式碼註釋改為全英文樣式（450 處），符合專案規範

---

## [1.1.1] - 2026-08-21

### 🐛 Bug 修復

1. **繁體中文語言檢測問題**
   - 問題：語言代碼被截斷導致繁體用戶看到簡體介面
   - 原因：`.split('-')[0]` 將 `zh-TW` 截斷為 `zh`
   - 解決：優先匹配完整語言代碼，再回退到主語言代碼

### 🎨 UI 改進

1. **圖標統一化**
   - Modal 彈窗：📁 emoji → `folder` Lucide 圖標（藍色）
   - 全局預設警告：⚠️ emoji → `alert-triangle` Lucide 圖標
   - 統一使用 Obsidian 原生圖標系統

### 🌏 國際化

1. **新增繁體中文翻譯 (zh-TW)**
   - 227 個完整翻譯鍵
   - 涵蓋所有功能模組
   - 符合台灣、香港、澳門地區使用習慣

### 支援語言

現已支援 **6 種語言**：
- English (en)
- 简体中文 (zh)
- 繁體中文 (zh-TW) ⭐ 新增
- 日本語 (ja)
- 한국어 (ko)
- Español (es)

---

## [1.1.0] - 2026-08-21

### 主要更新

#### 🚀 新增功能

1. **字体导入优化**
   - 替换浏览器原生文件选择器为 Obsidian Modal 弹窗
   - 支持拖拽导入字体文件
   - 支持点击选择文件（在 Modal 内部触发，避免用户激活限制）
   - 更好的视觉反馈和用户体验

2. **防抖机制**
   - 新增 `_debouncedDisplay()` 方法（300ms 延迟）
   - 避免短时间内多次刷新界面导致抖动
   - 优化按钮交互体验

3. **多语言支持增强**
   - 新增 `importing`（导入中...）翻译键
   - 新增 `importError`（导入失败）翻译键
   - 支持 5 种语言：英文、中文、日文、韩文、西班牙文

#### 🔧 技术改进

1. **代码架构优化**
   - 新增 `FontImportModal` 类（继承自 Obsidian Modal）
   - 新增 `importFontsFromFiles()` 辅助方法
   - 移除旧的 `importFont()` 方法（异步上下文调用导致错误）

2. **文件组织**
   - 将导入逻辑从 UI 层分离到 Plugin 层
   - 统一文件处理流程

3. **错误处理增强**
   - 完善错误捕获和日志输出
   - 优雅降级处理

#### 🐛 Bug 修复

1. **文件选择器用户激活错误**
   - 问题：浏览器报错 "File chooser dialog can only be shown with a user activation"
   - 原因：在异步上下文中调用 `input.click()` 导致用户激活丢失
   - 解决：使用 Obsidian Modal 弹窗方案，完全避免浏览器限制

2. **界面抖动问题**
   - 问题：点击导入、转换、重新扫描按钮后页面跳动
   - 原因：所有按钮都调用 `this.display()` 重新渲染整个页面
   - 解决：实现防抖机制，短时间内多次调用只执行最后一次

3. **重复方法清理**
   - 删除旧的异步 `importFont()` 方法
   - 统一使用新的模块化导入流程

### 技术细节

#### 新增文件
- `FontImportModal` 类（约 120 行）
- `importFontsFromFiles()` 方法（约 30 行）

#### 修改统计
- **新增**：约 300 行代码
- **删除**：约 50 行旧代码
- **修改**：约 20 处调用点

#### 核心改动
1. `FontImportModal` 类（main.js Line 2595+）
2. `importFontsFromFiles()` 方法（main.js Line 1611+）
3. 防抖机制（main.js Line 2637-2666）
4. 导入按钮重构（main.js Line 3860+）
5. 翻译键补充（5 种语言）

### 兼容性

- ✅ 向后兼容现有配置
- ✅ 支持桌面端和移动端
- ✅ Obsidian 最低版本：0.15.0

### 已知问题

无

### 升级说明

1. 更新插件文件（main.js、manifest.json）
2. 在 Obsidian 中重新加载插件
3. 测试导入字体功能（点击"导入字体"按钮应弹出拖拽窗口）

### 贡献者

- CoreVortex (@corevortex)

---

## [1.0.0] - 2026-08-20

初始版本发布
