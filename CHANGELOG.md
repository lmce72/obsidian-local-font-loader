# Changelog

## [1.5.0] - 2026-09-15

### ✨ 新增

1. **自訂數學字體改為修正 MathJax 的度量表**
   - MathJax 不在執行時讀取字體檔，而是用一張離線預製的度量表排版，它產生的 CSS
     只是這張表的投影。過去只換了「用哪個字體畫字形」，沒換「按什麼尺寸排」，
     導致字形來自使用者的字體、幾何卻來自 MathJax 的字體
   - 現在改用 canvas 逐字量出所選字體**實際渲染**的度量，寫回
     `MathJax.config.chtml.font`（24 個變體、3391 個字形）與 `delimiters` 的 HDW，
     再讓 MathJax 用它自己的機制重算幾何
   - 度量只隨字體變更計算一次，之後渲染不增加任何額外開銷
   - 外掛卸載或更換字體時會還原 MathJax 的原始度量（該物件為全域共用）

2. **數學字體合規檢測**
   - 設定頁在數學字體旁顯示警告圖示，並以警告框列出偏離的度量項目
   - 判據取自 MathJax 自帶 TeX 字體的度量（Computer Modern 體系）

3. **筆記標題與屬性面板套用 UI 字體**
   - `.inline-title`、筆記屬性面板與標籤一併納入 UI 字體範圍

### 🐛 Bug 修復

4. **修復根號橫槓與鉤子脫開**
   - MathJax 為 `√` 預留的空間（上下各 0.8em / 0.2em）是照它自己字體的墨跡算的
   - 換字體後墨跡高度不同，橫槓因此浮在鉤子上方
   - 度量表修正後，預留空間等於字體實際墨跡

5. **修復上下標與底數浮離**
   - 排版依度量表為每個斜體變數預留寬度，換字體後寬度不符，上下標逐層累積偏移

6. **修復下花括號斷成一串方塊**
   - 拉伸分隔符由**私有區碼位**拼成（如 `\underbrace` 使用 U+E152/E153/E154/E156）
   - 這些字形只存在於 MathJax 的 TeX 字體，任何一般數學字體都沒有
   - 改為讓拼接件維持 MathJax 自身的字體

7. **修復重新掃描／轉換字體後下拉選單不更新**
   - 兩個操作只更新了資料卻未重繪設定頁，新字體因此不出現在選單中

### 🔧 其他

- 拉伸尺寸變體（TEX-S1 ~ TEX-S4）不再被字體覆寫規則命中，以免高伸內容的幾何被破壞
- UI 字體規則涵蓋元素由 16 個增至 27 個以上

## [1.4.0] - 2026-09-14

### ✨ 新增

1. **設備預設命名改用「設備本身叫什麼」**
   - 桌面端使用系統主機名，行動端使用機型（如 `PJZ110`、`iPad`）
   - 原本多台機器一律顯示為 `Desktop-Linux`、`Mobile-Android`，無法分辨
   - 兩者皆取不到時才回退為「平台 + 系統」形式
   - 僅自動生成的名稱會隨規則更新，使用者自訂的名稱永不被覆蓋

2. **設備副行顯示主機名／機型**
   - 即使設備已被重新命名，仍能看出它實際是哪一台機器
   - 顯示名若已等於該標識則不重複顯示

3. **新增 iPadOS 識別**
   - 系統判定區分為 iOS、iPadOS、macOS 三態，並各自對應圖示
   - 使用 Obsidian 的 `Platform.isIosApp` 與 `Platform.isTablet`，而非 UA 嗅探
   - iPadOS 13 起 iPad 會自報桌面級 `Macintosh` UA，純 UA 嗅探必然誤判為 macOS

### 🐛 Bug 修復

4. **修復通知（Notice）等浮動 UI 未套用 UI 字體**
   - `.notice` 不在 UI 字體規則內，走繼承鏈落到 Obsidian 核心介面字體
   - 同類浮動 UI 一併納入：tooltip、命令面板、補全建議、懸浮預覽、編輯器補全面板

5. **修復 Android 無機型 UA 被誤解析出 `Linux`**
   - 原解析倒序掃描區塊，把同區塊中的 `Linux` 當成機型
   - 改為錨定 `Android <版本>` 段，取其後首個有效段（跳過語言標記與 `wv`）

6. **修復同型號設備可能共用同一設備 ID**
   - 指紋由 UA、解析度、時區、語言、核心數雜湊而成，同型號設備完全相同
   - 遷移時兩台設備會認領到同一 ID 並永久混為一台
   - 改為「認領即封口」：設備取得 ID 後，帳本中指向該 ID 的條目立即刪除
   - 已持有 ID 的設備在啟動時同樣封口，避免舊版本遺留條目被他人認領

7. **修復設備本地 ID 寫入失敗未被察覺**
   - 寫入後未校驗，靜默失敗會導致每次啟動都換新 ID
   - 現在寫入後立即回讀比對，不一致則明確輸出錯誤

8. **修復元信息中已廢棄欄位永不清除**
   - 結構變更（如移除 `osVersion`）不再參與比較，舊欄位會永久殘留
   - 現在鍵集合不一致即視為變更並重寫

### 🔧 其他

- 移除系統版本號：不再偵測、儲存或顯示，升級系統不會再造成元信息變動
- UI 字體規則統一為共用模板 `_buildUiFontRules()`，覆蓋元素由 16 個增至 27 個
- 設備身份改以唯一標識為唯一判準，名稱／機型僅用於顯示，不參與匹配

## [1.3.0] - 2026-09-14

### ✨ 新增

1. **設備唯一標識改為持久化 UUID**
   - 舊版以「UA + 螢幕解析度 + 時區 + 語言 + CPU 核心數」雜湊作為身份
   - 這些因子在旋轉、分屏、DPI 變化、夏令時下都會漂移，導致幾乎每次啟動都註冊一台「新設備」
   - 現在改為首次執行時生成 UUID 並存入**設備本地存儲**（不隨 data.json 同步），重啟不變
   - 舊指紋表僅作一次性遷移線索，已安裝的設備保留原有 ID 與預設綁定

2. **桌面端預設設備名改用主機名**
   - 多台桌機原本一律顯示為 `Desktop-Linux`／`Desktop-Windows`，無法分辨
   - 現在桌面端讀取系統主機名作為預設名稱；僅自動生成的名稱會隨升級刷新，使用者自訂的名稱不會被覆蓋

3. **設備型號顯示**
   - 設備條目新增型號副行：Android 顯示 UA 機型（如 `SM-G991B`），iOS 顯示機型族與系統版本（如 `iPhone · iOS 17.0`）
   - 作業系統圖示改為讀取設備自報的元信息，不再從設備名推斷

4. **新增「清理未綁定設備」入口**
   - 位於全域預設區，附確認對話框並逐條列出將被移除的設備
   - 僅清理非本機且未被任何預設綁定的條目

### 🐛 Bug 修復

5. **修復移動端行內代碼未套用代碼字體**
   - 使用者 CSS 片段以 `:root` 前綴取得更高特異性，壓過插件的 `body …` 規則
   - 片段硬編碼的字體名在移動端不存在，回退後落到系統通用 `monospace`

6. **修復移動端多行代碼塊行號未套用代碼字體**
   - 行號元素此前完全沒有規則，繼承的是正文文字字體
   - 現已涵蓋 Code Styler 行號與編輯器原生行號（`.cm-gutterElement`）

7. **修復 `var(--font-monospace)` 未生效**
   - Obsidian 核心把外觀字體設定寫成 `<body>` 的**內聯樣式**，而插件僅在 `:root` 宣告同名變數
   - `:root` 對 `<body>` 及其後代不構成競爭，導致所有讀取該變數的規則都解析到核心設定值
   - 現在等寬變數同時宣告於 `<body>` 作用域

8. **修復 Android／iOS 設備被誤判為 Linux／Mac**
   - Android 的 UA 同時含 `Linux`、iOS 的 UA 同時含 `Mac OS X`
   - 判定順序導致 `Android`／`iOS` 兩個分支永不可達，設備一律被命名為 `Mobile-Linux`
   - 現在 iOS 先於 macOS、Android 先於 Linux 判定

### 🔧 其他

- 統一程式碼字體規則的產生邏輯：行內代碼、代碼塊、行號由同一模板輸出，優先級只在一處決定
- 移除程式碼註解中的中文，改為純英文註解

## [1.2.5] - 2026-08-28

### 🐛 Bug 修復

1. **修復插件升級後配置不熱重載**
   - 升級版本後需重新啟動才能套用新配置
   - 現在會自動偵測配置變更並重新載入

## [1.2.4] - 2026-08-21

### 🐛 Bug 修復

1. **修復「轉換所有字體」功能失效**
   - 轉換流程發生錯誤但介面仍顯示「✓ 所有字體已轉換」的成功提示
   - 現在會正確輸出每個字體的實際轉換結果與錯誤訊息

2. **修復刪除目前編輯中的預設後設定頁消失**
   - 刪除正在編輯的預設後，設定頁下半部不再空白
   - 自動回退到目前設備的預設並完整渲染

3. **修復關閉設定頁後同步不再自動刷新**
   - 關閉設定頁後，雲端同步的變更不再自動顯示
   - 重新開啟設定頁即正確反映最新狀態

4. **修復複製預設時克隆錯誤內容**
   - 複製任何預設都會誤克隆「目前設備的預設」
   - 現在正確複製所選預設的內容

5. **修復特殊字元字體名稱破壞樣式**
   - 字體名稱含引號或反斜線時，可能破壞 CSS 樣式
   - 現在會自動轉義，安全生成 `@font-face` 與 `font-family`

6. **修復重新掃描時缺失字體被靜默移除**
   - 來源檔案被刪除的字體在重新掃描後不再消失
   - 改為保留條目並標示「缺失」狀態，供使用者手動處理

7. **修復刪除「僅快取」字體失敗**
   - 來源檔案已不存在的字體無法被刪除，殘留於列表
   - 現在可正常清理快取與條目

8. **修復儲存設定與同步重載的競態**
   - 快速修改多項設定時可能被舊資料回滾，或輸入框焦點丟失
   - 加入儲存中守衛，避免重載干擾正在編輯的內容

9. **統一 Latin 字體判定邏輯**
   - 單獨轉換與批次轉換的 unicode-range 判定不再不一致
   - 一律以目前設備預設的 Latin 字體設定為準

10. **多項資料防禦與資源清理**
    - 設定資料缺失 `fonts` 欄位時不再崩潰
    - 插件卸載時徹底清除注入的樣式與圖示

---

## [1.2.1] - 2026-08-21

### 📦 重新打包正式版本

1. **修復正式版 Release 資產過期問題**
   - GitHub 上原有的 `v1.2.0` Release 建立於設備管理重構之前，資產為舊版程式碼
   - `v1.2.1` 重新打包全部最新程式碼，確保資產與原始碼一致

### 🌏 文件更新

1. **README 新增繁體中文版本**
   - 完整繁体中文章節（概覽、功能、安裝、使用、效能、設定、授權等）
   - 頂部語言切換欄支援 6 種語言

### 🔐 發佈流程

1. **新增 GitHub Actions 發佈簽章流程**
   - 推標籤時自動建立 GitHub Release 並上傳插件資產
   - 使用 `attest-build-provenance` 為資產附加加密來源證明（Artifact Attestations）

### 📝 程式碼規範

1. **全英文註釋**
   - 所有程式碼註釋維持全英文樣式，符合專案規範

---

## [1.2.0] - 2026-08-21

### 🛠️ 設備管理重構

1. **設備與預設一對一綁定**
   - 一台設備僅能關聯且僅能關聯一個預設
   - 移除設備時自動從所有預設中清除關聯

2. **新設備自動註冊**
   - 插件啟動時偵測到與上次不同的設備，自動產生新設備並寫入 `data.json`
   - 無需手動重新載入插件即可在跨設備之間同步

3. **設備識別改用裝置指紋（平台 + UA/螢幕/時區/語言/CPU 雜湊）**
   - 設備 ID 持久化儲存在 `data.json`（可隨 vault 雲端同步）
   - 捨棄以 localStorage 作為設備標識的臨時方案
   - 支援多桌面端、多手機、多平板，無寫死邏輯

4. **修復同一物理設備出現重複 / 幽靈條目**
   - 清理 `deviceNameMap` 中重複的設備紀錄
   - 已識別設備名稱遺失時自動恢復

### 🔄 同步與刷新

1. **data.json 即時監聽**
   - 雲端同步系統推送 `data.json` 後，設備管理無需重啟插件即自動刷新

2. **設備管理工具列新增「重新整理」按鈕**
   - 使用 `refresh-cw` Lucide 圖示，可手動強制重新載入設定並重新渲染

### 🎨 UI 修復

1. **非當前設備顯示作業系統圖示**
   - 依設備平台類型（桌面 / 行動）與設備名稱推斷並顯示對應 OS 圖示

2. **「移動」按鈕圖示改為白色**
   - 與 Obsidian 原生按鈕樣式一致，hover 時恢復強調色

### 📝 程式碼規範

1. **全英文註釋**
   - 所有程式碼註釋改為全英文樣式（450 處），符合專案規範

---

## [1.1.1] - 2026-08-21

### 🐛 Bug 修復

1. **繁體中文語言檢測問題**
   - 問題：語言代碼被截斷導致繁體用戶看到簡體介面
   - 原因：`.split('-')[0]` 將 `zh-TW` 截斷為 `zh`
   - 解決：優先匹配完整語言代碼，再回退到主語言代碼

### 🎨 UI 改進

1. **圖標統一化**
   - Modal 彈窗：📁 emoji → `folder` Lucide 圖標（藍色）
   - 全局預設警告：⚠️ emoji → `alert-triangle` Lucide 圖標
   - 統一使用 Obsidian 原生圖標系統

### 🌏 國際化

1. **新增繁體中文翻譯 (zh-TW)**
   - 227 個完整翻譯鍵
   - 涵蓋所有功能模組
   - 符合台灣、香港、澳門地區使用習慣

### 支援語言

現已支援 **6 種語言**：
- English (en)
- 简体中文 (zh)
- 繁體中文 (zh-TW) ⭐ 新增
- 日本語 (ja)
- 한국어 (ko)
- Español (es)

---

## [1.1.0] - 2026-08-21

### 主要更新

#### 🚀 新增功能

1. **字体导入优化**
   - 替换浏览器原生文件选择器为 Obsidian Modal 弹窗
   - 支持拖拽导入字体文件
   - 支持点击选择文件（在 Modal 内部触发，避免用户激活限制）
   - 更好的视觉反馈和用户体验

2. **防抖机制**
   - 新增 `_debouncedDisplay()` 方法（300ms 延迟）
   - 避免短时间内多次刷新界面导致抖动
   - 优化按钮交互体验

3. **多语言支持增强**
   - 新增 `importing`（导入中...）翻译键
   - 新增 `importError`（导入失败）翻译键
   - 支持 5 种语言：英文、中文、日文、韩文、西班牙文

#### 🔧 技术改进

1. **代码架构优化**
   - 新增 `FontImportModal` 类（继承自 Obsidian Modal）
   - 新增 `importFontsFromFiles()` 辅助方法
   - 移除旧的 `importFont()` 方法（异步上下文调用导致错误）

2. **文件组织**
   - 将导入逻辑从 UI 层分离到 Plugin 层
   - 统一文件处理流程

3. **错误处理增强**
   - 完善错误捕获和日志输出
   - 优雅降级处理

#### 🐛 Bug 修复

1. **文件选择器用户激活错误**
   - 问题：浏览器报错 "File chooser dialog can only be shown with a user activation"
   - 原因：在异步上下文中调用 `input.click()` 导致用户激活丢失
   - 解决：使用 Obsidian Modal 弹窗方案，完全避免浏览器限制

2. **界面抖动问题**
   - 问题：点击导入、转换、重新扫描按钮后页面跳动
   - 原因：所有按钮都调用 `this.display()` 重新渲染整个页面
   - 解决：实现防抖机制，短时间内多次调用只执行最后一次

3. **重复方法清理**
   - 删除旧的异步 `importFont()` 方法
   - 统一使用新的模块化导入流程

### 技术细节

#### 新增文件
- `FontImportModal` 类（约 120 行）
- `importFontsFromFiles()` 方法（约 30 行）

#### 修改统计
- **新增**：约 300 行代码
- **删除**：约 50 行旧代码
- **修改**：约 20 处调用点

#### 核心改动
1. `FontImportModal` 类（main.js Line 2595+）
2. `importFontsFromFiles()` 方法（main.js Line 1611+）
3. 防抖机制（main.js Line 2637-2666）
4. 导入按钮重构（main.js Line 3860+）
5. 翻译键补充（5 种语言）

### 兼容性

- ✅ 向后兼容现有配置
- ✅ 支持桌面端和移动端
- ✅ Obsidian 最低版本：0.15.0

### 已知问题

无

### 升级说明

1. 更新插件文件（main.js、manifest.json）
2. 在 Obsidian 中重新加载插件
3. 测试导入字体功能（点击"导入字体"按钮应弹出拖拽窗口）

### 贡献者

- CoreVortex (@corevortex)

---

## [1.0.0] - 2026-08-20

初始版本发布
